import { SpacedCurrencyPipe } from './spaced-currency';

describe('SpacedCurrencyPipe', () => {
  it('should create an instance', () => {
    const directive = new SpacedCurrencyPipe();
    expect(directive).toBeTruthy();
  });
});
